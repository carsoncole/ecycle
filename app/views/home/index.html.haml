#home.container
  .row
    .col-sm
      .about
        %h1 Ecycle Today!
        %p 
          Support Bainbridge Scouting and Troop 1496 by recycling your old electronics! We want your unused iPhones, iPads, printers and small appliances which will be recycled. Simply drop-off or schedule a pickup! Donations appreciated.
        = link_to about_path do
          Learn More
          = icon('fas', 'arrow-right')


      .date-block
        %h3.mb-0
          Saturday
          %br
          June 1, 2019
        - if @dropoffs_start_at.present? && @dropoffs_end_at.present?
          Dropoffs:
          = @dropoffs_start_at.strftime('%l:%M%P')
          \-
          = @dropoffs_end_at.strftime('%l:%M%P')
          First Baptist Church
          %br
        - if @pickups_start_at.present? && @pickups_end_at.present?
          Pickups:
          = @pickups_start_at.strftime('%l:%M%P')
          \-
          = @pickups_end_at.strftime('%l:%M%P')

    .col-sm.title
      %h2.text-center.mb-0
        Schedule a Pickup
      %h5.text-center.mt-0
        - if @pickups_start_at.present? && @pickups_end_at.present?
          Between
          = @pickups_start_at.strftime('%l:%M%P')
          and
          = @pickups_end_at.strftime('%l:%M%P')

      = render 'form'

    .col-sm
      .text-center
        = image_tag "ecycle3.jpg", class: 'img-fluid rounded'

  .row
    .col-md-12
      .text-center= render 'facebook_button'


= render 'json-tld-event'